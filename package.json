{
    "name": "domobserver",
    "version": "0.0.1",
    "devDependencies": {
        "mocha": "*",
        "chai" : "*",
        "karma": "~0.10",
        "karma-mocha": "~0.1"
    },
    "scripts": {
        "test": "./node_modules/.bin/karma start --browsers Firefox --single-run"
    }
}
